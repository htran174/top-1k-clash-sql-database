CR_API_KEY=
PLAYER_TAG=

# Local (docker / localhost)
DATABASE_URL_LOCAL=postgresql+psycopg2://clash:clash@127.0.0.1:5432/clash

# Cloud SQL (Postgres)
DATABASE_URL_CLOUD=postgresql+psycopg2://clash_app:<PASSWORD>@<CLOUD_SQL_PUBLIC_IP>:5432/clash_meta?sslmode=require

GCS_BUCKET=
GCS_PREFIX=
